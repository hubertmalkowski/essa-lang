let power = fn base exp =>
    let multiply = fn times result =>
        if times == 0 then
            result
        else
            multiply (times - 1) (result * base)
    in
    multiply exp 1

debug (power 2 2)
